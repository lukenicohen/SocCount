# SocialCount Wordpress plugin

SocCount is very simple: It exposes a custom field on any post or page, which is kept up-to-date with the number of Facebook and Twitter shares for that page (by URI).
